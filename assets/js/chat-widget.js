/**
 * M06 chat widget — vanilla JS, no build step, no framework dependency.
 * Built out incrementally: state (M06 plan WP3), REST client (WP4),
 * UI/accessibility (WP5), lifecycle wiring (WP6).
 */
( function () {
	'use strict';

	// Populated by later work packages.
} )();
