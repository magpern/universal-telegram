; Silences E_DEPRECATED for third-party dev-tooling (PHPCompatibility) that
; is not yet PHP 8.1-clean internally. Scoped to this dev/CI image only;
; production plugin code is not written to rely on deprecated behaviour.
error_reporting = E_ALL & ~E_DEPRECATED
memory_limit = 512M
